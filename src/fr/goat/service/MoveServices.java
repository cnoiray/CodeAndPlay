package fr.goat.service;

public class MoveServices {

    // public static Player forward(Player pPlayer) {
    // Player newPlayer = new Player();
    // newPlayer.setX((pPlayer.getX()+2));
    // newPlayer.setY(pPlayer.getY());
    // return newPlayer;
    // }
    //
    // public static Player brake(Player pPlayer) {
    // Player newPlayer = new Player();
    // newPlayer.setX((pPlayer.getX()+1));
    // newPlayer.setY(pPlayer.getY());
    // return newPlayer;
    // }
    //
    // public static Player left(Player pPlayer) {
    // Player newPlayer = new Player();
    // newPlayer.setX(pPlayer.getX()+1);
    // newPlayer.setY(pPlayer.getY()-1);
    // return newPlayer;
    // }
    //
    // public static Player right(Player pPlayer) {
    // Player newPlayer = new Player();
    // newPlayer.setX(pPlayer.getX()+1);
    // newPlayer.setY(pPlayer.getY()+1);
    // return newPlayer;
    // }
    //
    // public static Player bonus(Player pPlayer) {
    // Player newPlayer = new Player();
    // newPlayer.setX(pPlayer.getX()+2);
    // newPlayer.setY(pPlayer.getY());
    // return newPlayer;
    // }
}
